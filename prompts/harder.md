Ask me a harder question on the same topic as the last one.

Push into the part I'd get wrong: an edge case, an interaction with something else
I've learned, or a "what breaks if..." variation. Don't just reword the previous
question.

One question. Wait for my answer, then grade it and call `aby_record_quiz` as usual.
